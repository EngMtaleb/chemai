"""chemai - a reusable toolkit for chemical engineering AI projects.

The package is deliberately thin. Each module answers one question:

    data/       where do the numbers come from, and are they what we expect?
    features/   what form does the physics require?
    models/     the estimator, its uncertainty, and its operating envelope
    evaluation/ does it work, and where does it fail?

Project-specific code lives in projects/, not here.
"""
__version__ = "0.1.0"
